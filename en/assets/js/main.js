
function setFixedSize(element){
    element.height(element.height());
    element.width(element.width());
}

function setWidth(element,width){
    element.width(width);
}

function setHeight(element,height){
    element.height(height);
}